# 源码模块边界

当前模块化原型由 `app/index.html` 加载下列角色模块。根目录 `index.html` 保留为 v0.1 基线：

```text
src/
├── shared/       # 布局、按钮、表格、状态标签、模拟数据
├── enterprise/   # 企业端页面与流程
├── worker/       # 个人端页面与流程
└── admin/        # 运营端页面与流程
```

每次新增页面时，优先放入对应端口目录；只有三端复用的内容才放入 `shared/`。`app.js` 只负责路由状态与应用框架，不应继续承载业务页面。
