# 源码模块边界

当前 `index.html` 是可直接浏览的 v0.1 原型基线。后续功能改造按以下边界迁移，避免继续向单一文件堆积：

```text
src/
├── shared/       # 布局、按钮、表格、状态标签、模拟数据
├── enterprise/   # 企业端页面与流程
├── worker/       # 个人端页面与流程
└── admin/        # 运营端页面与流程
```

每次新增页面时，优先放入对应端口目录；只有三端复用的内容才放入 `shared/`。
